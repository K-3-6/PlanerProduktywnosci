<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="PlanerMobile.MainPage"
             BackgroundColor="#F4F6F9"
             Title="Planer Mobilny">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <!-- Nagłówek -->
            <Label Text="Menedżer Zadań" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" TextColor="#2C3E50"/>

            <!-- Sekcja szybkiego dodawania -->
            <Frame BorderColor="#E0E0E0" CornerRadius="8" Padding="15" BackgroundColor="White">
                <VerticalStackLayout Spacing="10">
                    <Label Text="SZYBKIE DODAWANIE ZADANIA" FontSize="12" FontAttributes="Bold" TextColor="#7F8C8D"/>

                    <Entry x:Name="EntTitle" Placeholder="Wpisz tytuł zadania..." PlaceholderColor="#BDC3C7"/>
                    <Entry x:Name="EntDescription" Placeholder="Opis (opcjonalnie)..." PlaceholderColor="#BDC3C7"/>

                    <Button x:Name="BtnAdd" Text="DODAJ DO BAZY" BackgroundColor="#2ECC71" TextColor="White" FontAttributes="Bold" Clicked="BtnAdd_Clicked"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Przyciski sterujące -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                <Button x:Name="BtnRefresh" Text="Odśwież listę" BackgroundColor="#34495E" TextColor="White" Clicked="BtnRefresh_Clicked"/>
            </HorizontalStackLayout>

            <!-- Lista zadań z bazy -->
            <Label Text="TWOJE ZADANIA:" FontSize="14" FontAttributes="Bold" TextColor="#2C3E50" Margin="0,10,0,0"/>

            <CollectionView x:Name="ColTasks">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,5">
                            <Frame BorderColor="#BDC3C7" CornerRadius="10" Padding="15" BackgroundColor="White">
                                <HorizontalStackLayout HorizontalOptions="FillAndExpand">
                                    <VerticalStackLayout WidthRequest="200" Spacing="2">
                                        <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold" TextColor="#2C3E50"/>
                                        <Label Text="{Binding Description}" FontSize="12" TextColor="#7F8C8D"/>
                                        <HorizontalStackLayout Spacing="10" Margin="0,5,0,0">
                                            <Label Text="{Binding Category}" FontSize="10" FontAttributes="Bold" TextColor="#3498DB"/>
                                            <Label Text="{Binding Priority}" FontSize="10" FontAttributes="Bold" TextColor="#E74C3C"/>
                                        </HorizontalStackLayout>
                                    </VerticalStackLayout>

                                    <!-- Przycisk usuwania na karcie -->
                                    <Button Text="X" BackgroundColor="#E74C3C" TextColor="White" FontAttributes="Bold" 
                                            HeightRequest="35" WidthRequest="35" CornerRadius="17"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=DeleteTaskCommand}"
                                            CommandParameter="{Binding .}"/>
                                </HorizontalStackLayout>
                            </Frame>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>